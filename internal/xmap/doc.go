// Package xmap 在 Go 值与文档值之间来回搬运。
//
// 字段名、主键、引用、内嵌这些规则写在结构体的 bson 标签里，见 [parseTag]。
// 每个结构体类型只解析一次标签，结果缓存成一份取值方案（[plan]），
// 后续编解码都照着它走。
//
// 两条约定值得先知道：字段名的匹配**不分大小写**（按大写折叠后比），
// 而字符串默认会去掉首尾空白、空串变成 Null——这两条都由 [Mapper] 上的开关控制。
package xmap
